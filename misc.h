#ifndef __MISC
#define __MISC

#include <iostream>
#include "def.h"

void printArr(const vec<ld>& );
void printArr(const vec<vec<ld>>& );
void shape(std::string, const vec<vec<ld>>&);

#endif